# RAG Lite - No ML dependencies
# ==============================
# Keyword-based search only (works on 512MB RAM)

# FastAPI Server
fastapi>=0.100.0
uvicorn[standard]>=0.25.0
pydantic>=2.0.0

# OpenAI GPT-4 (for chatbot only)
openai>=1.0.0

# Utilities
python-dotenv>=1.0.0
httpx>=0.24.0
orjson>=3.9.0

# CORS & Streaming
python-multipart>=0.0.6
sse-starlette>=1.0.0
